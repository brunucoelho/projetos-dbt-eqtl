version: 2

sources:
  - name: horus
    database: eqtlinfo_raw
    schema: horus
    config:
      freshness:
        warn_after: {count: 1, period: day}
        error_after: {count: 2, period: day}
    tables:
      - name: mod_equipes
        config:
          loaded_at_field: "CAST(data_dados AS timestamp_tz)"