SELECT *
FROM (
    
SELECT 

QT.INDICADOR,
QT.DISTRIBUIDORA,
QT.REGIONAL,
QT.POLO,
QT.FORNECEDOR,
QT.CONTRATO,
QT.COMPETENCIA,
QT.QTD_TOTAL,
/*NVL(QA.QTD_A,0) QTD_A,
NVL(QB.QTD_B,0) QTD_B,
NVL(QC.QTD_C,0) QTD_C,
NVL(QD.QTD_D,0) QTD_D,*/
(NVL(QA.QTD_A,0)+NVL(QB.QTD_B,0)) A_B,
TRUNC(((NVL(QA.QTD_A,0)+NVL(QB.QTD_B,0))/QTD_TOTAL),4) RESULTADO,
'D-R-P-F' NIVEL,
('MEPE COMERCIAL'||'-'||QT.DISTRIBUIDORA||'-'||QT.REGIONAL||'-'||QT.FORNECEDOR)DESCRICAO,
M.MINIMO,
M.META,
M.DESAFIO,
'MAIOR MELHOR' POLARIDADE


FROM
(SELECT
'DESENVOLVIMENTO DE EQUIPES UPS' INDICADOR,
A.EMPRESA DISTRIBUIDORA,
A.REGIONAL_PRX REGIONAL,
A.POLO_PRX POLO,
A.FORNECEDOR,
''CONTRATO,
A.COMPETENCIA,
NVL(COUNT(A.EQUIPE),0) QTD_TOTAL

FROM (SELECT DISTINCT * FROM (SELECT DISTINCT * FROM {{ ref('resultado_mepe') }})) A 
WHERE A.Mepe_Final is not null
 
 GROUP BY 
A.EMPRESA,
A.REGIONAL_PRX,
A.POLO_PRX,
A.FORNECEDOR,
A.COMPETENCIA) QT

LEFT JOIN SB_PERFORMANCE.EQTL_CORP.METAS_ACTIO M ON M.DISTRIBUIDORA=QT.DISTRIBUIDORA AND M.REGIONAL= QT.REGIONAL and m.INDICADOR_KPI = 'MEPE GLOBAL' 

LEFT JOIN (SELECT
'DESENVOLVIMENTO DE EQUIPES UPS' INDICADOR,A.EMPRESA DISTRIBUIDORA,
A.REGIONAL_PRX REGIONAL,A.POLO_PRX POLO,
A.FORNECEDOR,''CONTRATO,
A.COMPETENCIA, COUNT(A.EQUIPE) QTD_A
FROM (SELECT DISTINCT * FROM (SELECT DISTINCT * FROM {{ ref('resultado_mepe') }})) A 
WHERE A.CLASSE_MEPE_FINAL = 'A'  GROUP BY A.EMPRESA,A.REGIONAL_PRX,A.POLO_PRX,A.FORNECEDOR,
A.COMPETENCIA,A.CLASSE_MEPE_FINAL) QA ON QA.DISTRIBUIDORA = QT.DISTRIBUIDORA
                                     AND QA.REGIONAL = QT.REGIONAL
                                     AND QA.POLO = QT.POLO
                                     AND QA.FORNECEDOR = QT.FORNECEDOR
                                     AND QA.COMPETENCIA =  QT.COMPETENCIA
                                     
LEFT JOIN (SELECT
'DESENVOLVIMENTO DE EQUIPES UPS' INDICADOR,A.EMPRESA DISTRIBUIDORA,
A.REGIONAL_PRX REGIONAL,A.POLO_PRX POLO,
A.FORNECEDOR,''CONTRATO,
A.COMPETENCIA, COUNT(A.EQUIPE) QTD_B
FROM (SELECT DISTINCT * FROM (SELECT DISTINCT * FROM {{ ref('resultado_mepe') }})) A 
WHERE A.CLASSE_MEPE_FINAL = 'B'  GROUP BY A.EMPRESA,A.REGIONAL_PRX,A.POLO_PRX,A.FORNECEDOR,
A.COMPETENCIA,A.CLASSE_MEPE_FINAL) QB ON QB.DISTRIBUIDORA = QT.DISTRIBUIDORA
                                     AND QB.REGIONAL = QT.REGIONAL
                                     AND QB.POLO = QT.POLO
                                     AND QB.FORNECEDOR = QT.FORNECEDOR
                                     AND QB.COMPETENCIA =  QT.COMPETENCIA  
                                     
LEFT JOIN (SELECT
'DESENVOLVIMENTO DE EQUIPES UPS' INDICADOR,A.EMPRESA DISTRIBUIDORA,
A.REGIONAL_PRX REGIONAL,A.POLO_PRX POLO,
A.FORNECEDOR,''CONTRATO,
A.COMPETENCIA, COUNT(A.EQUIPE) QTD_C
FROM (SELECT DISTINCT * FROM (SELECT DISTINCT * FROM {{ ref('resultado_mepe') }})) A 
WHERE A.CLASSE_MEPE_FINAL = 'C'  GROUP BY A.EMPRESA,A.REGIONAL_PRX,A.POLO_PRX,A.FORNECEDOR,
A.COMPETENCIA,A.CLASSE_MEPE_FINAL) QC ON QC.DISTRIBUIDORA = QT.DISTRIBUIDORA
                                     AND QC.REGIONAL = QT.REGIONAL
                                     AND QC.POLO = QT.POLO
                                     AND QC.FORNECEDOR = QT.FORNECEDOR
                                     AND QC.COMPETENCIA =  QT.COMPETENCIA       
                                                                     
LEFT JOIN (SELECT
'DESENVOLVIMENTO DE EQUIPES UPS' INDICADOR,A.EMPRESA DISTRIBUIDORA,
A.REGIONAL_PRX REGIONAL,A.POLO_PRX POLO,
A.FORNECEDOR,''CONTRATO,
A.COMPETENCIA, COUNT(A.EQUIPE) QTD_D
FROM (SELECT DISTINCT * FROM (SELECT DISTINCT * FROM {{ ref('resultado_mepe') }})) A 
WHERE A.CLASSE_MEPE_FINAL = 'D'  GROUP BY A.EMPRESA,A.REGIONAL_PRX,A.POLO_PRX,A.FORNECEDOR,
A.COMPETENCIA,A.CLASSE_MEPE_FINAL) QD ON QD.DISTRIBUIDORA = QT.DISTRIBUIDORA
                                     AND QD.REGIONAL = QT.REGIONAL
                                     AND QD.POLO = QT.POLO
                                     AND QD.FORNECEDOR = QT.FORNECEDOR
                                     AND QD.COMPETENCIA =  QT.COMPETENCIA     
                                     
UNION ALL


SELECT 

QT.INDICADOR,
QT.DISTRIBUIDORA,
QT.REGIONAL,
QT.POLO,
'N/A' FORNECEDOR,
'N/A' CONTRATO,
QT.COMPETENCIA,
QT.QTD_TOTAL,
(NVL(QA.QTD_A,0)+NVL(QB.QTD_B,0)) A_B,
TRUNC(((NVL(QA.QTD_A,0)+NVL(QB.QTD_B,0))/QTD_TOTAL),4) RESULTADO,
'D-R-P' NIVEL,
('MEPE COMERCIAL'||'-'||QT.DISTRIBUIDORA||'-'||QT.REGIONAL)DESCRICAO,
M.MINIMO,
M.META,
M.DESAFIO,
'MAIOR MELHOR' POLARIDADE


FROM
(SELECT
'DESENVOLVIMENTO DE EQUIPES UPS' INDICADOR,
A.EMPRESA DISTRIBUIDORA,
A.REGIONAL_PRX REGIONAL,
A.POLO_PRX POLO,
'N/A' FORNECEDOR,
'N/A' CONTRATO,
A.COMPETENCIA,
NVL(COUNT(A.EQUIPE),0) QTD_TOTAL

FROM (SELECT DISTINCT * FROM (SELECT DISTINCT * FROM {{ ref('resultado_mepe') }})) A
WHERE A.Mepe_Final is not null
 
 GROUP BY 
A.EMPRESA,
A.REGIONAL_PRX,
A.POLO_PRX,
A.COMPETENCIA) QT


LEFT JOIN SB_PERFORMANCE.EQTL_CORP.METAS_ACTIO M ON M.DISTRIBUIDORA=QT.DISTRIBUIDORA AND M.REGIONAL= QT.REGIONAL and m.INDICADOR_KPI = 'MEPE GLOBAL' 



LEFT JOIN (SELECT
'DESENVOLVIMENTO DE EQUIPES UPS' INDICADOR,A.EMPRESA DISTRIBUIDORA,
A.REGIONAL_PRX REGIONAL,A.POLO_PRX POLO,
'N/A' FORNECEDOR,
'N/A' CONTRATO,
A.COMPETENCIA, COUNT(A.EQUIPE) QTD_A
FROM (SELECT DISTINCT * FROM (SELECT DISTINCT * FROM {{ ref('resultado_mepe') }})) A 
WHERE A.CLASSE_MEPE_FINAL = 'A'  GROUP BY A.EMPRESA,A.REGIONAL_PRX,A.POLO_PRX,
A.COMPETENCIA,A.CLASSE_MEPE_FINAL) QA ON QA.DISTRIBUIDORA = QT.DISTRIBUIDORA
                                     AND QA.REGIONAL = QT.REGIONAL
                                     AND QA.POLO = QT.POLO
                                   --AND QA.FORNECEDOR = QT.FORNECEDOR
                                     AND QA.COMPETENCIA =  QT.COMPETENCIA
                                     
LEFT JOIN (SELECT
'DESENVOLVIMENTO DE EQUIPES UPS' INDICADOR,A.EMPRESA DISTRIBUIDORA,
A.REGIONAL_PRX REGIONAL,A.POLO_PRX POLO,
'N/A' FORNECEDOR,
'N/A' CONTRATO,
A.COMPETENCIA, COUNT(A.EQUIPE) QTD_B
FROM (SELECT DISTINCT * FROM (SELECT DISTINCT * FROM {{ ref('resultado_mepe') }})) A 
WHERE A.CLASSE_MEPE_FINAL = 'B'  GROUP BY A.EMPRESA,A.REGIONAL_PRX,A.POLO_PRX,
A.COMPETENCIA,A.CLASSE_MEPE_FINAL) QB ON QB.DISTRIBUIDORA = QT.DISTRIBUIDORA
                                     AND QB.REGIONAL = QT.REGIONAL
                                     AND QB.POLO = QT.POLO
                                     --AND QB.FORNECEDOR = QT.FORNECEDOR
                                     AND QB.COMPETENCIA =  QT.COMPETENCIA  
                                     
LEFT JOIN (SELECT
'DESENVOLVIMENTO DE EQUIPES UPS' INDICADOR,A.EMPRESA DISTRIBUIDORA,
A.REGIONAL_PRX REGIONAL,A.POLO_PRX POLO,
'N/A' FORNECEDOR,
'N/A' CONTRATO,
A.COMPETENCIA, COUNT(A.EQUIPE) QTD_C
FROM (SELECT DISTINCT * FROM (SELECT DISTINCT * FROM {{ ref('resultado_mepe') }})) A 
WHERE A.CLASSE_MEPE_FINAL = 'C'  GROUP BY A.EMPRESA,A.REGIONAL_PRX,A.POLO_PRX,
A.COMPETENCIA,A.CLASSE_MEPE_FINAL) QC ON QC.DISTRIBUIDORA = QT.DISTRIBUIDORA
                                     AND QC.REGIONAL = QT.REGIONAL
                                     AND QC.POLO = QT.POLO
                                     --AND QC.FORNECEDOR = QT.FORNECEDOR
                                     AND QC.COMPETENCIA =  QT.COMPETENCIA       
                                                                     
LEFT JOIN (SELECT
'DESENVOLVIMENTO DE EQUIPES UPS' INDICADOR,A.EMPRESA DISTRIBUIDORA,
A.REGIONAL_PRX REGIONAL,A.POLO_PRX POLO,
'N/A' FORNECEDOR,'N/A' CONTRATO,
A.COMPETENCIA, COUNT(A.EQUIPE) QTD_D
FROM (SELECT DISTINCT * FROM (SELECT DISTINCT * FROM {{ ref('resultado_mepe') }})) A 
WHERE A.CLASSE_MEPE_FINAL = 'D'  GROUP BY A.EMPRESA,A.REGIONAL_PRX,A.POLO_PRX,
A.COMPETENCIA,A.CLASSE_MEPE_FINAL) QD ON QD.DISTRIBUIDORA = QT.DISTRIBUIDORA
                                     AND QD.REGIONAL = QT.REGIONAL
                                     AND QD.POLO = QT.POLO
                                     --AND QD.FORNECEDOR = QT.FORNECEDOR
                                     AND QD.COMPETENCIA =  QT.COMPETENCIA     
                                     
                                     
UNION ALL
-- D-R

SELECT 

QT.INDICADOR,
QT.DISTRIBUIDORA,
QT.REGIONAL,
'N/A' POLO,
'N/A' FORNECEDOR,
'N/A' CONTRATO,
QT.COMPETENCIA,
QT.QTD_TOTAL,
(NVL(QA.QTD_A,0)+NVL(QB.QTD_B,0)) A_B,
TRUNC(((NVL(QA.QTD_A,0)+NVL(QB.QTD_B,0))/QTD_TOTAL),4) RESULTADO,
'D-R' NIVEL,
('MEPE COMERCIAL'||'-'||QT.DISTRIBUIDORA)DESCRICAO,
M.MINIMO,
M.META,
M.DESAFIO,
'MAIOR MELHOR' POLARIDADE


FROM
(SELECT
'DESENVOLVIMENTO DE EQUIPES UPS' INDICADOR,
A.EMPRESA DISTRIBUIDORA,
A.REGIONAL_PRX REGIONAL,
'N/A' POLO,
'N/A' FORNECEDOR,
'N/A' CONTRATO,
A.COMPETENCIA,
NVL(COUNT(A.EQUIPE),0) QTD_TOTAL

FROM (SELECT DISTINCT * FROM (SELECT DISTINCT * FROM {{ ref('resultado_mepe') }})) A
WHERE A.Mepe_Final is not null
/*WHERE A.POLO_PRX = 'GRAJAU'
AND A.COMPETENCIA = '202305'*/
 
 GROUP BY 
A.EMPRESA,
A.REGIONAL_PRX,
A.COMPETENCIA) QT


LEFT JOIN SB_PERFORMANCE.EQTL_CORP.METAS_ACTIO M ON M.DISTRIBUIDORA=QT.DISTRIBUIDORA AND M.REGIONAL=QT.REGIONAL and m.INDICADOR_KPI = 'MEPE GLOBAL' 

LEFT JOIN (SELECT
'DESENVOLVIMENTO DE EQUIPES UPS' INDICADOR,A.EMPRESA DISTRIBUIDORA,
A.REGIONAL_PRX REGIONAL,'N/A' POLO,
'N/A' FORNECEDOR,
'N/A' CONTRATO,
A.COMPETENCIA, COUNT(A.EQUIPE) QTD_A
FROM (SELECT DISTINCT * FROM (SELECT DISTINCT * FROM {{ ref('resultado_mepe') }})) A 
WHERE A.CLASSE_MEPE_FINAL = 'A'  GROUP BY A.EMPRESA,A.REGIONAL_PRX,
A.COMPETENCIA,A.CLASSE_MEPE_FINAL) QA ON QA.DISTRIBUIDORA = QT.DISTRIBUIDORA
                                     AND QA.REGIONAL = QT.REGIONAL
                                   --  AND QA.POLO = QT.POLO
                                   --AND QA.FORNECEDOR = QT.FORNECEDOR
                                     AND QA.COMPETENCIA =  QT.COMPETENCIA
                                     
LEFT JOIN (SELECT
'DESENVOLVIMENTO DE EQUIPES UPS' INDICADOR,A.EMPRESA DISTRIBUIDORA,
A.REGIONAL_PRX REGIONAL,'N/A' POLO,
'N/A' FORNECEDOR,
'N/A' CONTRATO,
A.COMPETENCIA, COUNT(A.EQUIPE) QTD_B
FROM (SELECT DISTINCT * FROM (SELECT DISTINCT * FROM {{ ref('resultado_mepe') }})) A 
WHERE A.CLASSE_MEPE_FINAL = 'B'  GROUP BY A.EMPRESA,A.REGIONAL_PRX,
A.COMPETENCIA,A.CLASSE_MEPE_FINAL) QB ON QB.DISTRIBUIDORA = QT.DISTRIBUIDORA
                                     AND QB.REGIONAL = QT.REGIONAL
                                     --AND QB.POLO = QT.POLO
                                     --AND QB.FORNECEDOR = QT.FORNECEDOR
                                     AND QB.COMPETENCIA =  QT.COMPETENCIA  
                                     
LEFT JOIN (SELECT
'DESENVOLVIMENTO DE EQUIPES UPS' INDICADOR,A.EMPRESA DISTRIBUIDORA,
A.REGIONAL_PRX REGIONAL,'N/A' POLO,
'N/A' FORNECEDOR,
'N/A' CONTRATO,
A.COMPETENCIA, COUNT(A.EQUIPE) QTD_C
FROM (SELECT DISTINCT * FROM (SELECT DISTINCT * FROM {{ ref('resultado_mepe') }})) A 
WHERE A.CLASSE_MEPE_FINAL = 'C'  GROUP BY A.EMPRESA,A.REGIONAL_PRX,
A.COMPETENCIA,A.CLASSE_MEPE_FINAL) QC ON QC.DISTRIBUIDORA = QT.DISTRIBUIDORA
                                     AND QC.REGIONAL = QT.REGIONAL
                                     --AND QC.POLO = QT.POLO
                                     --AND QC.FORNECEDOR = QT.FORNECEDOR
                                     AND QC.COMPETENCIA =  QT.COMPETENCIA       
                                                                     
LEFT JOIN (SELECT
'DESENVOLVIMENTO DE EQUIPES UPS' INDICADOR,A.EMPRESA DISTRIBUIDORA,
A.REGIONAL_PRX REGIONAL,'N/A' POLO,
'N/A' FORNECEDOR,
'N/A' CONTRATO,
A.COMPETENCIA, COUNT(A.EQUIPE) QTD_D
FROM (SELECT DISTINCT * FROM (SELECT DISTINCT * FROM {{ ref('resultado_mepe') }})) A 
WHERE A.CLASSE_MEPE_FINAL = 'D'  GROUP BY A.EMPRESA,A.REGIONAL_PRX,
A.COMPETENCIA,A.CLASSE_MEPE_FINAL) QD ON QD.DISTRIBUIDORA = QT.DISTRIBUIDORA
                                     AND QD.REGIONAL = QT.REGIONAL
                                     --AND QD.POLO = QT.POLO
                                     --AND QD.FORNECEDOR = QT.FORNECEDOR
                                     AND QD.COMPETENCIA =  QT.COMPETENCIA     

                                
                                     
UNION ALL
-- D

SELECT 

QT.INDICADOR,
QT.DISTRIBUIDORA,
'N/A' REGIONAL,
'N/A' POLO,
'N/A' FORNECEDOR,
'N/A' CONTRATO,
QT.COMPETENCIA,
QT.QTD_TOTAL,
(NVL(QA.QTD_A,0)+NVL(QB.QTD_B,0)) A_B,
TRUNC(((NVL(QA.QTD_A,0)+NVL(QB.QTD_B,0))/QTD_TOTAL),4) RESULTADO,
'D' NIVEL,
('MEPE COMERCIAL'||'-'||QT.DISTRIBUIDORA)DESCRICAO,
M.MINIMO,
M.META,
M.DESAFIO,
'MAIOR MELHOR' POLARIDADE


FROM
(SELECT
'DESENVOLVIMENTO DE EQUIPES UPS' INDICADOR,
A.EMPRESA DISTRIBUIDORA,
'N/A'  REGIONAL,
'N/A' POLO,
'N/A' FORNECEDOR,
'N/A' CONTRATO,
A.COMPETENCIA,
NVL(COUNT(A.EQUIPE),0) QTD_TOTAL

FROM (SELECT DISTINCT * FROM (SELECT DISTINCT * FROM {{ ref('resultado_mepe') }})) A
WHERE A.Mepe_Final is not null
/*WHERE A.POLO_PRX = 'GRAJAU'
AND A.COMPETENCIA = '202305'*/
 
 GROUP BY 
A.EMPRESA,
A.COMPETENCIA) QT

LEFT JOIN  SB_PERFORMANCE.EQTL_CORP.METAS_ACTIO M ON M.DISTRIBUIDORA=QT.DISTRIBUIDORA AND M.REGIONAL IS  NULL and m.INDICADOR_KPI = 'MEPE GLOBAL' 

LEFT JOIN (SELECT
'DESENVOLVIMENTO DE EQUIPES UPS' INDICADOR,A.EMPRESA DISTRIBUIDORA,
'N/A'  REGIONAL,
'N/A' POLO,
'N/A' FORNECEDOR,
'N/A' CONTRATO,
A.COMPETENCIA, COUNT(A.EQUIPE) QTD_A
FROM (SELECT DISTINCT * FROM (SELECT DISTINCT * FROM {{ ref('resultado_mepe') }})) A 
WHERE A.CLASSE_MEPE_FINAL = 'A'  GROUP BY A.EMPRESA,A.COMPETENCIA,A.CLASSE_MEPE_FINAL) QA ON QA.DISTRIBUIDORA = QT.DISTRIBUIDORA
                                   --  AND QA.REGIONAL = QT.REGIONAL
                                   --  AND QA.POLO = QT.POLO
                                   --AND QA.FORNECEDOR = QT.FORNECEDOR
                                     AND QA.COMPETENCIA =  QT.COMPETENCIA
                                     
LEFT JOIN (SELECT
'DESENVOLVIMENTO DE EQUIPES UPS' INDICADOR,A.EMPRESA DISTRIBUIDORA,
'N/A'  REGIONAL,
'N/A' POLO,
'N/A' FORNECEDOR,
'N/A' CONTRATO,
A.COMPETENCIA, COUNT(A.EQUIPE) QTD_B
FROM (SELECT DISTINCT * FROM (SELECT DISTINCT * FROM {{ ref('resultado_mepe') }})) A 
WHERE A.CLASSE_MEPE_FINAL = 'B'  GROUP BY A.EMPRESA,A.COMPETENCIA,A.CLASSE_MEPE_FINAL) QB ON QB.DISTRIBUIDORA = QT.DISTRIBUIDORA
                                    -- AND QB.REGIONAL = QT.REGIONAL
                                     --AND QB.POLO = QT.POLO
                                     --AND QB.FORNECEDOR = QT.FORNECEDOR
                                     AND QB.COMPETENCIA =  QT.COMPETENCIA  
                                     
LEFT JOIN (SELECT
'DESENVOLVIMENTO DE EQUIPES UPS' INDICADOR,A.EMPRESA DISTRIBUIDORA,
'N/A'  REGIONAL,
'N/A' POLO,
'N/A' FORNECEDOR,
'N/A' CONTRATO,
A.COMPETENCIA, COUNT(A.EQUIPE) QTD_C
FROM (SELECT DISTINCT * FROM (SELECT DISTINCT * FROM {{ ref('resultado_mepe') }})) A 
WHERE A.CLASSE_MEPE_FINAL = 'C'  GROUP BY A.EMPRESA,A.COMPETENCIA,A.CLASSE_MEPE_FINAL) QC ON QC.DISTRIBUIDORA = QT.DISTRIBUIDORA 
                                     --AND QC.REGIONAL = QT.REGIONAL
                                     --AND QC.POLO = QT.POLO
                                     --AND QC.FORNECEDOR = QT.FORNECEDOR
                                     AND QC.COMPETENCIA =  QT.COMPETENCIA       
                                                                     
LEFT JOIN (SELECT
'DESENVOLVIMENTO DE EQUIPES UPS' INDICADOR,A.EMPRESA DISTRIBUIDORA,
'N/A'  REGIONAL,
'N/A' POLO,
'N/A' FORNECEDOR,
'N/A' CONTRATO,
A.COMPETENCIA, COUNT(A.EQUIPE) QTD_D
FROM (SELECT DISTINCT * FROM (SELECT DISTINCT * FROM {{ ref('resultado_mepe') }})) A 
WHERE A.CLASSE_MEPE_FINAL = 'D'  GROUP BY A.EMPRESA,A.COMPETENCIA,A.CLASSE_MEPE_FINAL) QD ON QD.DISTRIBUIDORA = QT.DISTRIBUIDORA
                                     --AND QD.REGIONAL = QT.REGIONAL
                                     --AND QD.POLO = QT.POLO
                                     --AND QD.FORNECEDOR = QT.FORNECEDOR
                                     AND QD.COMPETENCIA =  QT.COMPETENCIA    
) WHERE TO_DATE(COMPETENCIA,'YYYYMM') = TO_DATE({{ get_month_ref() }},'YYYYMM')